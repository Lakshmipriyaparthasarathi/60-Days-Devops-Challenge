# Use official Ubuntu image as the base
FROM ubuntu:latest

# Set maintainer info (optional)
LABEL maintainer="lakshmi@example.com"

# Install updates and curl
RUN apt update && apt install -y curl

# Set the default command
CMD ["echo", "🎉 Hello from my custom Docker image!"]
